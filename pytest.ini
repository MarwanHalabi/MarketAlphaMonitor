[pytest]
addopts = -ra
markers =
    unit: Unit tests for isolated components.
    integration: Integration tests requiring multiple components.
    contract: Contract or API schema verification tests.
testpaths =
    market-tracker/tests
    yahoo_etl/tests
